# Server Actions

Use for mutations that stay on the **Next.js BFF** (create/update journal, trigger anchor pipeline).

Keep Prisma and viem calls server-side so secrets and consistency guarantees stay in one place.
